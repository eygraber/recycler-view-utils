if(!project.hasProperty('BINTRAY_USER')) project.ext.BINTRAY_USER = 'eygraber'
if(!project.hasProperty('ARTIFACT_GROUP')) project.ext.ARTIFACT_GROUP = 'com.staticbloc.recycler-view'
if(!project.hasProperty('BINTRAY_REPO')) project.ext.BINTRAY_REPO = 'recycler-view'
if(!project.hasProperty('BINTRAY_ORG')) project.ext.BINTRAY_ORG = 'staticbloc'